<!DOCTYPE html>
<html>
  <head>
    <title>Product Image</title>
  </head>
  <body>
    <h1><%= product.name %></h1>
    <p><%= product.description %></p>
    <p>Price: $<%= product.price %></p>
    <% if (product.pictureUrl) { %>
      <img src="<%= product.pictureUrl %>" alt="Product Image" style="max-width: 500px;">
    <% } else { %>
      <p>No image available</p>
    <% } %>
  </body>
</html>